version: "3.8"

x-service-volumes: &service-volumes
  - ./:/app/:rw,cached
  
services:
  website:
    image: burogu-apuri:latest
    command: python manage.py runserver 0.0.0.0:8000
    volumes: *service-volumes
    ports: 
      - "8000:8000"

